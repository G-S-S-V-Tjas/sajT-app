import 'package:flutter/material.dart';

class TjasApp extends StatelessWidget {
  const TjasApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
